query ReviewThread($id: ID!) {
  node(id: $id) {
    __typename
    ... on PullRequestReviewThread {
      id
      isResolved
      isOutdated
      comments(first: 100) {
        nodes {
          id
          databaseId
          body
          path
          line
          url
        }
      }
    }
  }
}
